<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
    <link href="{{{ cssdir }}}/vendor.css" rel="stylesheet" />
    <link href="{{{ cssdir }}}/style.css" rel="stylesheet" />

    <script src="{{{ jsdir }}}/vendors.js" type="text/javascript"></script>
    <script src="{{{ jsdir }}}/bundle.js" type="text/javascript"></script>
</head>
<body ng-app="test">
    <div class="container" ng-controller="hest as ctrl">
        <h1>{{ ctrl.val }}</h1>
        <div class="row">
            <div class="col-sm-6 col-sm-offset-3">
                <h1>Hello.</h1>
                <h5>I am a default page.</h5>
                <p>Please replace me with something nicer.</p>
                <a href="#" data-toggle="tooltip" title="Some tooltip text!">Hover over me</a>
            </div>
        </div>
    </div>
</body>
</html>